export class Date {
    constructor(data){
        this.id = data.id
        this.date = date.date
        this.explanation = data.explanation
        this.img = this.img || data.hdurl 
        this.title = this.title
    }
}